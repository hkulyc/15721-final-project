simple: |-
  while (true){{
    {body}
  }}

while: |-
  while ({condition}){{
    {body}
  }}

for: |- 
  for (int {name} = {start}; {name} <= {end}; {name} += {step}){{
    {body}
  }}


revfor: |- 
  for (int {name} = {start}; {name} >= {end}; {name} -= {step}){{
    {body}
  }}

